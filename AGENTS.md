# Codex 작업 루틴 (필독)

이 저장소의 **기준 문서(소스 오브 트루스)** 는 아래 경로의 파일들이다.  
`C:\Users\User\Downloads\files\` 의 원본은 **초기 입력용**으로만 두고, 앞으로 업데이트/기록은 **프로젝트 내부 기준본**에만 한다.

## 기준 문서
- `AI_지침.md` / `AI_지침_압축.md`
- `AI_메뉴얼.md` / `AI_메뉴얼_압축.md`
- `AI_장기기억.md` / `AI_장기기억_요약.md`
- 작업 관련 스킬 문서: `skills\*` (예: `skills\스킬_게임제작_v2.0.md`)

## 코딩 대상(프로젝트 기준본)
- 코딩/게임 파일: `games\*` (예: `games\해초농장_v8.5.html`)
- 분석 문서: `*_분석.md` (예: `해초농장_분석.md`)

---

## 매 작업 시작 시 (필수)

### 1) 체크리스트를 텍스트로 출력
아래 체크리스트를 **그대로 텍스트로 출력하지 않으면 작업을 시작하지 않은 것으로 간주**한다.

체크리스트(작업 시작 전)
☐ AI_메뉴얼 확인 완료  
☐ 관련 스킬 문서 확인 완료  
☐ 계획서 작성 완료  

체크리스트(작업 완료 후)
☐ AI_장기기억.md 업데이트 완료  
☐ 스킬 문서 생성/업데이트 완료  

### 2) Depth(읽기 깊이) 선택
- **Quick**: 압축본만 읽고 진행 (아주 단순/저위험)
- **Standard (기본값)**: 압축본 + 필요 구간 원본 추가 확인 (대부분 작업)
- **Deep**: 4개 원본을 확실히 검토 (새 작업 유형/고위험/복잡/실수 여지 큼)

선택한 Depth를 작업 시작 메시지에 명시한다.

### 3) 문서 읽기 순서(수정로그→지침→메뉴얼→장기기억→스킬)
아래 순서로 읽고, “읽었다”를 명시한다.

1. 수정로그: `AI_수정로그.md` (최근 어떤 AI가 어떤 .md를 수정했는지 맥락 파악)
2. 지침: `AI_지침_압축.md` (Deep이면 `AI_지침.md`도)
3. 메뉴얼: `AI_메뉴얼_압축.md` (Deep이면 `AI_메뉴얼.md`도)
4. 장기기억: `AI_장기기억_요약.md` (Deep이면 `AI_장기기억.md` 최근 로그도)
5. 스킬: 작업 유형에 맞는 `skills\*` (Deep이면 원본까지)

### 4) 코딩 작업 전(필수): 분석 문서 먼저
코딩/파일 수정 작업(HTML/CSS/JS 등)이라면, 실행 전에 반드시:
- 대상 파일을 프로젝트 기준본(`games\*`)으로 고정
- 분석 문서(`*_분석.md`)를 **먼저 작성/갱신**
- 분석 문서 안에 “전문가용(구조/라인맵/데이터흐름)” + “초보용(쉽게 설명)”을 포함
- 분석 중 발견한 문제는 즉시 수정하지 말고 **개선 후보**로만 적고, 별도 플랜+승인으로 진행

### 5) 실행 규칙(승인 전 실행 금지)
어떤 작업이든:
- **최대한 자세한 플랜을 먼저 제시**하고,
- **사용자 승인 후에만 실행**한다.

“간단한 작업”도 예외 없다. (파일 수정/명령 실행/결과물 생성 포함)

---

## 매 작업 완료 후 (필수)
- `AI_장기기억.md`에 로그 1줄 추가: **날짜 / 작업 유형 / 결과물 / 특이사항** (항목 시작 부분에 `[Claude]`, `[Gemini]` 등 출처 태그 포함)
- **`.md` 파일을 수정/생성했다면**, `AI_수정로그.md`에 기록 1줄 추가: **날짜 / AI 이름 / 대상 파일 / 변경 요약**
- 새 작업 유형: 스킬 문서 생성 (`skills\스킬_*.md`)
- 기존 작업 유형: 해당 스킬 문서 업데이트
- 보편적 실수/규칙: `AI_메뉴얼.md`에 **버전 올려** 기록 (임의 수정 금지)
- 사용자 지적/수정 또는 작업 중 발견한 “향후 도움이 될 개선사항(노하우)”이 있으면:
  - 먼저 대화에서 **개선 후보**로 정리
  - 반영 플랜(수정 대상 문서/변경 내용/버전 업 여부) 제시
  - **승인 후에만** `.md` 생성/수정으로 반영

---

## 문서(.md) 수정 추적 규칙
모든 AI(Claude, ChatGPT, Gemini 등)는 아래 규칙을 엄격히 준수한다.
- **문서 변경 추적**: `.md` 파일의 모든 수정 및 생성 내역은 `AI_수정로그.md`에 기록한다.
- **작업 성과 추적**: 작업 결과물 및 노하우는 `AI_장기기억.md`에 기록하며, 항목 시작 부분에 자신의 이름 태그(예: `[Claude]`, `[Gemini]`)를 명시한다.
- **코딩 파일 추적**: `.html`, `.js` 등의 코딩 파일명에는 작업한 AI의 이름을 포함한다 (예: `v8.6(Claude).html`).

---

## 소통 규칙
- 항상 한국어로 답한다.
- 전문용어는 반드시 괄호로 쉬운 설명을 추가한다.
- 불확실한 정보는 먼저 “확실하지 않지만…”으로 밝힌다.
- 정보 조사 시 2개 이상의 출처를 교차 검증한다.
- 결과물 제공 후, 사용자에게 “틀린 점/원하는 수정” 확인을 요청한다.
