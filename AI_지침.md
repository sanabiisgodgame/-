## 필수 체크리스트 (매 작업 시작 전 반드시 텍스트로 출력할 것)

작업 시작 전:
☐ AI_메뉴얼 확인 완료
☐ 관련 스킬 문서 확인 완료
☐ 계획서 작성 완료

작업 완료 후:
☐ AI_장기기억.md 업데이트 완료
☐ 스킬 문서 생성/업데이트 완료

→ 체크리스트를 출력하지 않으면 작업을 시작하지 않은 것으로 간주한다.

## 메뉴얼 관련
모든 작업 시작 전, 프로젝트에 있는 AI_메뉴얼을 반드시 읽을 것.
메뉴얼은 임의로 수정하지 않는다.
실수 발생 또는 새로운 노하우가 생긴 경우에만 버전을 올려 수정한다. (예: v1.1 → v1.2)

## 작업 실행 규칙
어떤 작업이든 계획서를 먼저 제시하고, 사용자 승인 후 실행한다.
"간단한 작업"이라도 예외 없음.

## 코딩(파일 수정) 선 분석 규칙
코딩/파일 수정 작업(HTML/CSS/JS 등)을 하기 전에는 반드시:
1) 대상 파일을 프로젝트 기준본으로 고정하고(필요 시 복사),
2) 분석 문서(`*_분석.md`)를 먼저 작성/갱신한 뒤,
3) 그 다음에 계획서 → 승인 → 실행 순서로 진행한다.

새 코딩 파일이 들어오면 “그 파일만” 보지 말고, 연관 문서(해당 스킬 문서/장기기억/지침 압축본/AGENTS 등)도 함께 점검해 **업데이트가 필요한지** 확인한다.  
단, 문서 생성/수정도 작업이므로 반드시 플랜을 제시하고 승인 후에만 반영한다.

## 스킬 문서 규칙
처음 하는 유형의 작업이면 완료 후 스킬 문서를 생성한다.
이미 스킬 문서가 있는 작업이면 완료 후 해당 문서를 업데이트한다.

## 장기기억 규칙
모든 작업 완료 후 AI_장기기억.md에 반드시 기록한다.
기록 항목: 날짜 / 작업 유형 / 결과물 / 특이사항
메뉴얼(규칙서)과 장기기억(로그)은 절대 혼용하지 않는다.

## 실수 기록 규칙
사용자가 잘못을 지적하면 즉시 인정하고,
보편적 실수 → 메뉴얼에 기록
작업 특화 실수 → 해당 스킬 문서에 기록

## 개선사항(노하우) 기록 규칙
사용자가 지적/수정했거나, 작업 중 발견한 “나중에 도움이 될 개선사항(노하우)”이 있으면:
1) 먼저 대화에서 **개선 후보**로 정리하고,
2) 문서 반영 플랜(수정 대상/변경 내용/버전 업 여부)을 제시한 뒤,
3) **사용자 승인 후에만** `.md` 파일을 생성/수정하여 반영한다.

## 언어 및 소통
항상 한국어로 답한다.
전문용어 사용 시 반드시 괄호 안에 쉬운 설명을 추가한다.
불확실한 정보는 "확실하지 않지만~"으로 먼저 밝힌다.

## 문서(.md) 수정 추적 규칙
1. **모든 `.md` 파일을 수정/생성할 때**, `AI_수정로그.md`에 한 줄 기록한다: `날짜 / AI 이름 / 대상 파일 / 변경 요약`
2. **장기기억 항목에 출처 태그**를 붙인다: 예) `[Claude]`, `[ChatGPT]`, `[Gemini]`, `[사용자]`
3. **코딩 파일**(`.html` 등)은 파일명에 AI명을 포함한다: 예) `v8.6(Claude).html`
4. 수정로그와 장기기억은 역할이 다르다:
   - `AI_수정로그.md` → **문서 변경 추적** (어떤 AI가 어떤 .md를 수정했는지)
   - `AI_장기기억.md` → **작업 성과 로그** (무엇을 만들었고 무엇을 배웠는지)

## 효율적 작업 흐름
매 세션 시작 시, 목적에 따라 읽기 순서를 최적화한다:
1. `AI_수정로그.md` → 최근 어떤 AI가 무엇을 수정했는지 파악 (맥락 복원)
2. `AI_지침_압축.md` → 기본 규칙 확인
3. `AI_장기기억_요약.md` → 프로젝트 이력 파악
4. 작업 대상의 `*_분석.md` → 코딩 대상 구조 파악
5. 관련 `skills\*` → 작업 유형별 노하우 참조
